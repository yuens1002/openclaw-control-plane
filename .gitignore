node_modules/
dist/
coverage/
.env
.env.local
*.local.md
*.tsbuildinfo

# Internal AI-assisted workflow tooling (hooks, commands, verification
# status) — process artifacts specific to how this repo's maintainer works
# with Claude Code, not project code or documentation a public contributor
# needs. Untracked going forward; historical versions remain in git log
# (see issue #102 for the decision not to also purge history).
/.claude/
