# Authenticated Typed-Work API Acceptance Criteria

Plan: `docs/plans/authenticated-typed-work-api/plan.md`

Status: implementation and independent QC complete against
`a0f7a793ca88962477665a97fd335f467defac43`; human review is pending.

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-PLAN-001 | D1 | project-manager | Planning contract | Inspect the three planning artifacts and coverage check. | Plan names concrete artifacts, one owner per deliverable, sessions, commit schedule, release gates, and non-goals; every deliverable D1-D11 has at least one AC and no AC names an unknown deliverable. | PASS - D1-D11 and all 30 AC rows mapped. | CONFIRMED - independent coverage and anti-drift gates passed. | Pending human review |
| AC-CONFIG-001 | D2 | security | Versioned configuration | Run schema fixtures for valid and invalid config. | A generic v1 fixture parses; duplicate `(issuer, subject)` identities, duplicate principal IDs, unknown roles, invalid delegation targets, unsupported providers/algorithms, unsafe names, and out-of-range clock skew fail with stable non-secret errors. | PASS - 14 configuration fixtures cover every named case. | CONFIRMED - exact-head config suite passed. | Pending human review |
| AC-CONFIG-002 | D2 | security | Production-mode guard | Start configuration validation in development and production modes. | Basic Auth requires an explicit development flag, is rejected in production, and production without at least one valid OIDC issuer/principal/policy mapping fails readiness before listening. | PASS - mode, mapping, Basic Auth, and HTTPS guards passed. | CONFIRMED - security review found no production guard blocker. | Pending human review |
| AC-AUTHN-001 | D3 | security | JWT verification | Verify deterministic signed fixtures for valid, expired, not-yet-valid, wrong-audience, wrong-issuer, unknown-key, malformed, and unsupported-algorithm tokens. | Only the valid token authenticates; every invalid token fails before principal resolution, authorization, or runtime execution and does not expose token contents or key material. | PASS - all credential variants and required claims passed. | CONFIRMED - clean exact-head security verdict. | Pending human review |
| AC-AUTHN-002 | D3 | security | JWKS rotation | Serve old/new keys from a deterministic local JWKS fixture and exercise cache refresh. | The new key is accepted after bounded refresh, a retired key is rejected after refresh, an unknown key cannot trigger unbounded fetches, and an unavailable required JWKS reports not-ready without accepting stale-unapproved identity. | PASS - real HTTP refresh, cooldown, retirement, key usability, and readiness fixtures passed. | CONFIRMED - bounded timing and verifier parity reviewed. | Pending human review |
| AC-IDENTITY-001 | D3 | security | Stable identity | Verify tokens with the same `(iss, sub)` and changed mutable claims, then tokens changing either stable claim. | Mutable display/email claims do not alter the configured principal or actor; changing issuer or subject requires a distinct configured mapping and otherwise fails closed. | PASS - mutable and stable-claim fixtures passed. | CONFIRMED - stable `(iss, sub)` mapping reviewed. | Pending human review |
| AC-AUTHZ-001 | D4 | security | Static RBAC decision | Exercise exact action/resource grants, complete `*` resource-ID wildcard, no match, partial glob, and unknown action. | Decisions always include a unique decision ID, allowed/denied result, policy version, and stable reason codes; only exact grants or the complete resource-ID wildcard allow access. | PASS - grant, wildcard, mismatch, partial-glob, and decision-ID fixtures passed. | CONFIRMED - exact-match RBAC reviewed. | Pending human review |
| AC-AUTHZ-002 | D4 | security | Delegation | Exercise configured and unconfigured on-behalf-of requests with roles that do and do not grant the same action. | Delegation is allowed only when both the authenticated principal's role and the delegation grant allow the action; trusted context retains both principals and derives the effective actor from configuration. | PASS - configured, missing, unauthorized-action, and no-role cases passed. | CONFIRMED - independent delegation matrix passed. | Pending human review |
| AC-TRUST-001 | D4 | security | Trust-field spoof resistance | Submit actor, principal, delegation, role, authorization, digest, request-ID, and timestamp fields through every command-capable payload surface. | Reserved fields are rejected or ignored according to the documented envelope and can never replace the server-derived trusted command context or server-owned metadata. | PASS - exhaustive top-level and nested surface matrices returned 400 before execution. | CONFIRMED - server-derived context boundary reviewed. | Pending human review |
| AC-DENIAL-001 | D4 | security | Bounded denial | Deny one command and one authorized-resource query through the policy boundary and inspect persisted state. | Each denial routes through the runtime service's bounded audit operation with principal/delegation, action, resource, policy, decision, reasons, origin, and request ID; no work item, action attempt, result, artifact, or projection effect is created. | PASS - command/query denials persisted audit-only records under pre-policy limits. | CONFIRMED - denial audit and no-effect invariants passed. | Pending human review |
| AC-WORK-001 | D5 | backend-architect | Typed intake lifecycle | Create a registered event and a work item derived from authorized record references, then attempt malformed, unknown, retired, and unauthorized variants. | Valid intake persists typed records and ordered provenance with server-derived attribution; every invalid variant fails before a partial record or edge is stored. | PASS - HTTP/PostgreSQL intake, replay, registry, authorization, and source-kind rollback cases passed. | CONFIRMED - independent lifecycle review passed. | Pending human review |
| AC-APPROVAL-001 | D5 | backend-architect | Immutable approval record | Authenticate an approver, record approve/reject decisions for a server-canonicalized command revision, and read the resulting record. | The stored approval binds principal, actor/delegation, authorization decision, operation, work item, revision, and server-computed digest; callers submit no trusted approver context and cannot mutate or reuse the record for a different command. | PASS - approved and rejected records persisted/read; exact binding and rejected execution passed. | CONFIRMED - final behavioral audit closed both decision paths. | Pending human review |
| AC-QUERY-001 | D5 | backend-architect | Kind-complete reads | Seed event, work item, action attempt, approval, non-artifact result, artifact, audit, edges, and projection records, then use the service query boundary. | Authorized callers can retrieve every seeded kind and traverse ordered provenance without direct repository access or consumer-specific schema knowledge. | PASS - every kind, projection, audit, and edge path passed through service/HTTP/tool boundaries. | CONFIRMED - unfiltered and kind-complete reads reviewed. | Pending human review |
| AC-QUERY-002 | D5 | backend-architect | Bounded pagination | Exercise first/next pages, kind/type/sequence filters, malformed cursors, excessive limits, and cross-resource authorization. | Results use stable ordering and opaque cursors, enforce a documented maximum page size, reject malformed bounds consistently, and never reveal an unauthorized record's existence. | PASS - opaque cross-stream cursor ordering, stream binding, limits, filters, and denial cases passed. | CONFIRMED - prior empty-filter SQL defect fixed and reverified. | Pending human review |
| AC-REGISTRY-001 | D5 | backend-architect | Registration lifecycle | Query active registrations and attempt commands against active, unknown, and retired versions. | The public catalog is bounded and non-secret; new commands accept only active exact versions, while unknown or retired versions fail before persistence with stable errors and historical records remain readable. | PASS - active catalog and active/unknown/retired command fixtures passed with 400 client errors. | CONFIRMED - HTTP allowed registration gap closed. | Pending human review |
| AC-HTTP-001 | D6 | backend-architect | Versioned authenticated API | Exercise all documented `/v1/runtime` routes with missing, invalid, allowed, and denied bearer credentials. | Operational routes require bearer identity in production, apply their declared authorization action/resource, return one stable error envelope, and expose no legacy unauthenticated write path. | PASS - every route's missing/invalid/allowed/denied matrix passed. | CONFIRMED - final registration allowed-path assertion passed. | Pending human review |
| AC-HTTP-002 | D6 | backend-architect | Idempotent command adapter | Retry an equal command during in-progress and terminal states, then reuse its key with changed content. | Equal retries preserve one operation reference and service result; changed canonical content returns the service conflict without a duplicate effect and retains conflict audit. | PASS - repository and HTTP inserted/replayed/conflict/audit paths passed. | CONFIRMED - no duplicate effect observed. | Pending human review |
| AC-HTTP-003 | D6 | backend-architect | Approval binding | Execute an approval-required operation by immutable approval ID, then independently change target, arguments, effects, revision, executor, and referenced approval. | The server rebuilds the RFC 8785 envelope/digest from the actual call and resolves trusted approver evidence from storage; only an exact valid approval executes and every mismatch fails before effects. | PASS - all six independent mutations returned 400 and record count stayed unchanged. | CONFIRMED - exact command/digest binding reviewed. | Pending human review |
| AC-HTTP-004 | D6 | backend-architect | Compatibility | Run the existing `/events`, root, health, and shell regression tests with development auth disabled/enabled as documented. | Existing valid event inserted/duplicate semantics remain available through the authenticated service boundary, health remains public, and no legacy pipeline stub bypasses production authorization. | PASS - root, health, shell, and event compatibility suites passed. | CONFIRMED - legacy writes remain fail-closed in production. | Pending human review |
| AC-TOOL-001 | D7 | backend-architect | Authenticated tool client | Invoke representative command, record, projection, provenance, and audit tools with a rotating async token provider and inspect requests. | Each call obtains and sends the current bearer token, uses the versioned HTTP contract, validates typed inputs/outputs, forwards no trusted-context field, and returns stable API failures without leaking the token. | PASS - seven adapter tests cover input/output parsing, token rotation, attribution, tools, transport, and redaction. | CONFIRMED - malformed input never reached fetch. | Pending human review |
| AC-ATTRIBUTION-001 | D6 | backend-architect | End-to-end attribution | Execute one non-artifact `example.*` result and one artifact-producing example through HTTP and read them through HTTP/tool queries. | Event/work causation, attempt, principal, actor/delegation, authorization decision, operation, target, request/tool IDs, inputs, results/artifacts, timestamps, outcome, and ordered edges remain traversable. | PASS - non-artifact HTTP and artifact tool flows preserve context, tool origin/ID, outputs, and edges. | CONFIRMED - exact-head artifact traversal passed. | Pending human review |
| AC-READY-001 | D8 | devops | Independent readiness | Inject failure separately for API startup, PostgreSQL, migrations, registry, identity/policy config, and required JWKS. | `/health` reports each dimension separately with overall not-ready/503 when any required dimension fails, returns ready/200 only when all required dimensions pass, and emits no secrets or private policy data. | PASS - six independent fixtures and degraded production-container health passed. | CONFIRMED - dimensions and non-secret output reviewed. | Pending human review |
| AC-DEPLOY-001 | D8 | devops | Portable production profile | Build and inspect API and optional worker targets plus production configuration. | The profile is provider-neutral, uses external secret references, starts migrations before serving, disables Basic Auth, keeps workflow registrations external, and does not alter the root OpenClaw service contract. | PASS - API and worker images built; profiles, guards, and workflow-neutral worker passed. | CONFIRMED - clean container/security review. | Pending human review |
| AC-SMOKE-001 | D8 | devops | Deploy/restart smoke | Against an empty PostgreSQL database and deterministic local issuer, start the production profile, execute an `example.*` command, query its result/projection, restart the service, and query again. | Migrations complete automatically, authenticated command/query succeed, and identical durable data and idempotency state survive restart. | PASS - production image + internal TLS JWKS completed migrate/command/query/restart/replay. | CONFIRMED - public verifier passed on exact code head. | Pending human review |
| AC-RECOVERY-001 | D8 | devops | Backup and restore | Populate the conformance graph, create a documented backup, restore into an empty database, and rerun reads/rebuild. | Restored records, edges, registrations, idempotency state, audit, and projections match the source; projection rebuild is deterministic and the recovery procedure names verification and rollback gates. | PASS - source/restored counts matched `4/4/1/1/1/16`; rebuild returned `{changed:true}`. | CONFIRMED - `pg_dump`/`pg_restore` verifier and runbook passed. | Pending human review |
| AC-TEST-001 | D9 | test-engineer | Public conformance suite | Run focused auth/security/API tests, PostgreSQL integration, container smoke/restart/recovery, then full suite. | Deterministic public fixtures cover every AC-AUTHN/AUTHZ/TRUST/DENIAL/QUERY/REGISTRY/HTTP/TOOL/ATTRIBUTION/READY/DEPLOY/SMOKE/RECOVERY invariant without a hosted vendor or private consumer dependency. | PASS - `360/360` plus production Docker/TLS/recovery verifier passed with generic fixtures. | CONFIRMED - independent behavioral audit found no blocker. | Pending human review |
| AC-TEST-002 | D9 | test-engineer | Regression and quality | Run full tests, typecheck, build, container build, production dependency audit, and `git diff --check` on the exact head. | Every command exits zero, no critical/high production vulnerability remains unexplained, and the exact reviewed SHA is recorded. | PASS - all release commands exited zero on `a0f7a79`; production audit found zero. | CONFIRMED - exact reviewed implementation SHA recorded in `review.md`. | Pending human review |
| AC-DOCS-001 | D10 | project-manager | Public API and operator docs | Review API/auth config, key rotation, startup migration, smoke, backup/restore, rollback, and worker guidance against code/tests. | Docs explain generic what/why and executable operations, match current routes/config/readiness, name no private consumer or live environment, and require no external unpublished process. | PASS - public auth/deployment/API/recovery guidance matches executable verifier. | CONFIRMED - generic-language audit passed. | Pending human review |
| AC-DOCS-002 | D10 | project-manager | ADR and repository consistency | Grep the full docs tree and relevant source comments for superseded unauthenticated/fail-closed/deployment claims. | ADR implementation status and all public docs agree with the shipped trust boundary and production profile; no stale claim says operational writes are universally unavailable or Basic Auth is production auth. | PASS - ADR/status/docs synchronization and private-reference grep passed. | CONFIRMED - planning placeholders replaced with executed evidence. | Pending human review |
| AC-REVIEW-001 | D11 | test-engineer | Exact-head independent review | Independently compare the current diff, implementation, tests, plan, ACs, ADRs, and public docs. | `review.md` records only executed evidence, identifies residual risks and exact SHA, and has no unresolved current-head blocker before human review or PR publication. | PASS - consolidated review records exact SHA, executed gates, verdict, and residual risks. | CONFIRMED - behavioral and security reviewers found no blockers. | Pending human review |

## Gate 1: Plan To AC Coverage

Every deliverable D1-D11 has at least one AC row. No AC references a
deliverable outside D1-D11. This must be re-run after any plan or AC revision.

## Gate 2: Anti-Drift

Pass conditions state behavioral invariants rather than tenant names, hosted
provider values, generated IDs, secrets, or one live environment. Fixtures use
only local deterministic keys and `example.*` names. This must be re-run before
implementation and during independent verification.
