# Client-Grade Railway Install ACs

| AC | Plan ref | Role | Pass invariant | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- |
| AC-INSTALL-001 | D2 | devops | A fresh run deploys the configured Railway template with generated `SETUP_PASSWORD` and `OPENCLAW_GATEWAY_TOKEN`. | PASS - mocked fresh install asserts `railway deploy -t clawdbot-railway-template` with supplied secrets. | PASS - checked `installOpenClawOnRailway` deploy path and green `tests/openclaw-railway-installer.test.ts`. | Approved - user review on 2026-08-13. |
| AC-INSTALL-002 | D2 | devops | A rerun detects an existing healthy template service and does not create a duplicate unless explicitly forced. | PASS - mocked existing `SUCCESS` service skips deploy. | PASS - test asserts no `deploy` command is called on healthy rerun. | Approved - user review on 2026-08-13. |
| AC-INSTALL-003 | D2 | devops | The installer waits for the newest deployment to reach `SUCCESS` before reporting readiness, and exits nonzero for terminal failure states. | PASS - mocked BUILDING-to-SUCCESS path and FAILED terminal path covered. | PASS - failure message includes actionable `railway logs --service ... --lines 200`. | Approved - user review on 2026-08-13. |
| AC-INSTALL-004 | D2 | devops | If the generated service domain targets the wrong port, the installer updates it to the configured target port before health verification. | PASS - mocked domain `3000` causes `domain update ... --port 8080`. | PASS - test checks update call before ready result. | Approved - user review on 2026-08-13. |
| AC-INSTALL-005 | D4 | devops | Setup URL, OpenClaw URL, service id, username, and setup password are written only to ignored local files. | PASS - mocked writes assert `.env.local` and `handoff.local.md` include handoff values. | PASS - `.gitignore` includes `.env.local` and `*.local.md`; output paths default to ignored files. | Approved - user review on 2026-08-13. |
| AC-INSTALL-006 | D3 | devops | The PowerShell entrypoint delegates to the testable installer path and fails clearly when Node/npm dependencies are missing. | PASS - script parser accepts wrapper; wrapper calls `npm exec -- tsx packages/openclaw-railway-installer/src/cli.ts`. | PASS - inspected wrapper and ran PowerShell syntax parse. | Approved - user review on 2026-08-13. |
| AC-TEST-001 | D5 | test-engineer | Mocked tests cover fresh install, idempotent rerun, failed deployment, port correction, and local output generation. | PASS - `tests/openclaw-railway-installer.test.ts` has five mocked installer tests. | PASS - `npm.cmd run test` passes 8 tests across 3 files. | Approved - user review on 2026-08-13. |
| AC-PUBLIC-001 | D7 | security | `.env.local` and `*.local.md` handoff files are ignored by git. | PASS - ignore patterns added. | PASS - checked `.gitignore`; final `git check-ignore` pending in verification pass. | Approved - user review on 2026-08-13. |
| AC-DOCS-001 | D6 | project-manager | Public docs describe the repeatable client install flow, local-only outputs, and live-test boundary. | PASS - root README and deploy README updated. | PASS - docs mention mocked CLI tests, opt-in live boundary, and ignored handoff outputs. | Approved - user review on 2026-08-13. |
