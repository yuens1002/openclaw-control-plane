# Live-Instance Operations Acceptance Criteria

**Branch:** `docs/live-instance-operations`
**Plan:** `docs/plans/live-instance-operations/plan.md`

## Context

This protocol governs how anyone — this repo's library code, a human at a
terminal, or an agent mid-session — may read or write state on an
already-provisioned live instance. It exists because two ungoverned live
operations occurred during an unrelated fix. The pass bar is therefore not
"the document reads well" but "would this have stopped what actually
happened," plus strict Public-Repo Rule compliance, since this repo is public
and the incident involved a private service and a live credential.

## Column Definitions

| Column | Filled by | When |
| --- | --- | --- |
| Plan ref | Author | At AC authoring; links each row to a plan deliverable ID. |
| Role | Author | At AC authoring; names the role that owns the deliverable. |
| Agent | Verification worker | During verification; PASS/FAIL with evidence. Never filled by the worker that implemented the row's deliverable. |
| QC | Orchestrating thread | After reading verification evidence; confirms or overrides. |
| Reviewer | Human reviewer | During manual review; final sign-off per AC. |

## Coverage Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-COV-001 | D5 | coordination | Plan exists and names the gated execution model | Read `docs/plans/live-instance-operations/plan.md` | Plan names branch, deliverables with owning role and stream, design decisions, execution model, commit schedule, dependencies, and out-of-scope items; the Dependencies section records the D7-approval gate on D8/D9 as a hard gate, not a suggestion | PASS - plan carries branch, deliverables with role+stream, design decisions, execution model, commit schedule, dependencies, out-of-scope; D7-approval gate on D8/D9 stated as a hard gate. | CONFIRMED - amended during QC: D4's row named three files while Files-to-Edit named two; ADR now explicitly one-way by design. | PENDING |
| AC-COV-002 | D6 | coordination | AC table exists with traceable workflow columns | Read `docs/plans/live-instance-operations/ACs.md` | Every AC row carries a valid Plan ref (D1–D9) and Role, plus Agent, QC, and Reviewer columns; no deliverable D1–D9 lacks at least one AC | PASS - every row carries Plan ref and Role; D1-D9 all covered. | CONFIRMED. | PENDING |

## Functional Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-FN-001 | D1 | security/ops | Classification is two-axis | Read the classification section of `docs/live-instance-operations.md` | Mutation tier and credential exposure are separate, independently-assigned axes; an operation can be benign on one and gated on the other, and the document states why a single axis is insufficient | PASS - S2 states the two classifications are independent; S2.2 mutation table and S2.3 credential axis are separately assigned; S2.6 justifies why one axis is insufficient in both directions and sets stricter-verdict-wins. | CONFIRMED - read S2.4/S2.6 directly. | PENDING |
| AC-FN-002 | D1 | security/ops | Deploy is its own mutation tier | Read the classification table | A raw deploy/redeploy command is classified distinctly from ordinary writes, and the rule for it reflects that it bypasses the PR-merge flow rather than treating it as a normal code change | PASS - `deploy` is its own row, justified by PR-merge bypass rather than write size; rule requires the full S5 procedure and forbids local working-tree state as source. | CONFIRMED - provision-client.ts one-shot-snapshot header verified at HEAD. | PENDING |
| AC-FN-003 | D1 | security/ops | Credential-flow rule forbids argument-line secrets | Read the credential-flow section | The rule requires secrets to reach a command via stdin or environment and never via an argument string, and cites the existing in-repo pattern that already does this rather than inventing a new mechanism | PASS - S3.1 requires stdin/env; S2.4 extends the ban to every tier including read; cites railway-variables.ts writeRailwayVariable passing the value as stdin, never in args. | CONFIRMED. | PENDING |
| AC-FN-004 | D1 | security/ops | Pre-flight declaration is specified and justified as additive | Read the pre-flight section | The declaration enumerates target, tier, prior read, credential path, reversibility, and whether tested library code already covers the operation; and the document explains why it is not redundant with a run-command permission prompt, referencing that such a prompt did not fire on the incident's deploy step | PASS - all six required elements present; S4.1 justifies additivity and notes the prompt did not fire on the deploy step. | CONFIRMED. | PENDING |
| AC-FN-005 | D1 | security/ops | Existing library paths take precedence over ad hoc use | Read the protocol against `docs/setup-profile-applier.md` | The protocol generalizes the existing "do not call the mutating endpoints outside the tested path" rule to the whole live surface, and does not narrow or weaken it | PASS - S4 step 6 generalizes the narrow applier rule to the whole live surface; git diff shows the original rule untouched and only additive text appended. | CONFIRMED. | PENDING |
| AC-FN-006 | D1 | security/ops | Prod-state change procedure exists | Read the prod-state procedure section | The procedure states how a live configuration or deploy change is proposed, reviewed, applied, verified after the fact, and rolled back; verification-after-write is required, not optional | PASS - S5 covers propose/review/apply/verify/record/roll-back; 5.4 headed 'required, not optional' and distinguishes deployment success from serving readiness. | CONFIRMED. | PENDING |
| AC-FN-007 | D1 | security/ops | Every researched gap is dispositioned exactly once | Cross-check the protocol and plan against the research findings | Each identified gap appears exactly once as in-scope, named-follow-up-with-recommended-fix, or explicitly-excluded; none is silently dropped, and none appears in two categories | PASS - S7 dispositions all nine gaps exactly once across in-scope / escalated / named-follow-up / excluded; G5 flagged first. | CONFIRMED. | PENDING |
| AC-FN-008 | D2 | tooling/devops | Tier markers present and accurate | Read each of the seven named modules | Every named module carries a tier-marker header referencing the protocol, and the tier assigned matches that module's actual behavior when read — not the tier that would be most convenient | PASS - all modules carry a tier header; worker expanded scope to 12 modules after finding 5 unlisted live-touching paths. | CONFIRMED-WITH-FIXES - corrected two overclaims the verifier caught: import-workspace-files provenance (gateway-stop is second-hand, not upstream-double-confirmed) and provision-client's 'every function is credential-bearing' (false for the two update fns). Typecheck clean, 173/173 tests pass. | PENDING |
| AC-FN-009 | D3 | security/ops | Destructive unused capability is escalated | Read the filed issue and the protocol's classification table | The capability is classified Forbidden in the table, and an issue proposes deletion on the grounds that it has no callers, rather than proposing a gate around unused code | FAIL - substance complete and correctly argued for deletion over gating, but no issue is filed; the artifact is a draft file. | RESOLVED-PASS - filing was withheld from the worker because publishing to a public repo is outward-facing; surfaced for human approval, approved, and filed as issue #36. Draft corrected during QC first (its reference count was self-inconsistent: 'three call sites' vs 'four references'; actual is 3 call sites across 5 references). | PENDING |
| AC-FN-010 | D4 | security/ops | Cross-references resolve without contradiction | Read all four documents together | Each cross-referenced document points to the protocol for the general rule and retains its own specific rule; no rule is restated in two places with different wording | FAIL - (a) the ADR carries no back-link; (b) setup-profile-applier.md still described the reset endpoint as callable via the tested path while the protocol calls it Forbidden. | CONFIRMED-AND-FIXED - (b) was a real contradiction, now reconciled: the applier doc states reset has no tested path, is Forbidden, and is slated for deletion. (a) OVERRIDDEN - docs/README.md states an ADR is not updated to match later state, so a back-link would violate repo convention; plan amended to one-way reference by design. | PENDING |
| AC-FN-011 | D7 | tooling/devops | Mount analysis is evidence-based | Read the analysis against the wrapper and gateway routing code | The analysis states, with file-level evidence rather than inference, whether the wrapper's auth gate can observe the gateway's base-path configuration; and recommends a single target end state rather than leaving both open | PASS - Q1 confirmed against code that the wrapper's auth gate cannot observe the app's base path (its whole input set is req.path, SETUP_PASSWORD, and the Authorization header); Q2 traced all four 404 classes; single target state recommended. | CONFIRMED - the base-path-blind finding settles the open question from planning: the exemption's correctness is independent of the app's config. | PENDING |
| AC-FN-012 | D7 | tooling/devops | Analysis touches no live state | Review the analysis method | The analysis is produced from code reading and already-public unauthenticated responses only; no authenticated live call and no write of any kind is made to produce it | PASS - produced from source reading plus unauthenticated fetches of the two pinned upstream refs; no authenticated call, no write, no credential use. | CONFIRMED. | PENDING |
| AC-FN-013 | D8 | tooling/devops | Documentation names the canonical entry point correctly | Read `deploy/openclaw-railway/README.md` and the `Dockerfile` comment block | No document claims the Control UI is served under a base path; the canonical entry point is stated as the root URL, and any prior prefixed claim is corrected rather than left alongside the new one | PASS - the deploy README and Dockerfile header now state the root path as canonical; the prior base-path claim is corrected in place rather than left alongside. | CONFIRMED. | PENDING |
| AC-FN-014 | D8b | tooling/devops | The setup page links to the canonical URL | Read the patch and its build-time assertion | The wrapper's setup-page link resolves to the root path, applied by the same `sed` + `grep -F` assertion pattern the file already uses, so the build fails loudly if upstream changes the matched text rather than silently skipping the patch | PASS - the setup-page link is rewritten to the root path by a `sed` patch with both a positive `grep -F` assertion and a negative one for the old form. | CONFIRMED - replayed against the pinned upstream source; all 15 build steps pass. | PENDING |
| AC-FN-015 | D8c | tooling/devops | Passively-fetched root paths are exempt before the revert lands | Read the exempt list against the paths the browser fetches without an auth header | The assistant-media route and the provider-icon assets are exempt; the pass condition is that no path the browser fetches passively can return an auth challenge under root-mount, not merely that two specific strings were added | PASS - provider icons and the assistant-media route are exempt; verified live that neither returns an auth challenge. | CONFIRMED-WITH-FIX - the first implementation matched assistant-media by prefix, which would also have exempted sibling routes such as `-foo`. Copilot caught it; tightened to an exact match and verified by execution that the sibling forms now block. | PENDING |
| AC-FN-016 | D9 | tooling/devops | The base-path exemption is removed, and only it | Read the diff | The prefixed-path exemption is gone; the root-level exemptions and the unconditional gateway-token injection are untouched, since removing those would regress separately-merged fixes | PASS - only the prefixed exemption is removed; the root-level exemptions and the unconditional gateway-token injection are untouched. | CONFIRMED - `/openclaw` now returns 401 live, and the pre-existing exemptions still serve 200. | PENDING |
| AC-FN-017 | D9 | tooling/devops | The revert never reaches a live target without its prerequisites | Confirm the revert and its prerequisites are in the same deployed change | The exemption removal, the setup-page link fix, and the added passive-fetch exemptions ship in a single deploy. The hazard D7 identified is a revert reaching production ahead of the link fix; co-shipping forecloses that outright and is strictly stronger than ordering separate commits, which only helps if the changes could deploy independently. Amended from an earlier wording that required commit ordering — that wording tested a proxy for the invariant rather than the invariant | PASS - all four changes shipped in a single deploy, so the revert could not reach the target ahead of its prerequisites. | CONFIRMED - deployed together as `e32a9b6a` from clean `main`, never from a local working tree, per the protocol deploy rule. | PENDING |
| AC-FN-018 | D10 | tooling/devops | Live verification passes after the gateway restart | Probe the live target after deploy | No path the browser fetches passively returns an auth challenge; the setup route and the dashboard document both still do; the reverted prefix is no longer specially handled. **Amended after the deploy:** the original wording required the root URL to return no auth challenge. That was wrong — the root document is the `SETUP_PASSWORD`-protected dashboard and correctly challenges, as it did before this change. The invariant that actually matters is that *passive* fetches never challenge, since those are the ones that cannot answer one and therefore re-fire the popup | PASS - manifest/favicon/sw.js/control-ui-config all 200 with no challenge; real provider icons 200 `image/svg+xml` with no challenge; assistant-media reached the app (404 without a source param) with no challenge; `/setup` and `/setup/api/status` 401 with challenge; `/openclaw` now 401, exemption gone; `/healthz` 200. | CONFIRMED - orchestrator-run against the live target after deploy `e32a9b6a`. Note an earlier probe used an invented icon filename (`ProviderIcon-openai.svg`, not in the upstream set) and 404'd; retested against real names from the pinned upstream tree. | PENDING |
| AC-FN-019 | D10 | tooling/devops | Persistent state survives the restart | Inspect the volume and instance state after deploy | The persistent volume is still attached at its mount path with its prior contents, and the instance reports configured — verified after the restart rather than assumed from the fact that volumes usually persist | PASS - volume attached at `/data`, service Online, 1.2 GB used (1.1 GB before the deploy; grew through normal operation rather than resetting). Instance serves its bootstrap config with agent identity and id intact. | CONFIRMED - checked after the restart, not inferred from volumes-usually-persist. This was the user's explicit concern before approving the deploy. | PENDING |

## Counterfactual Acceptance Criteria

These are the sharpest test: the protocol must demonstrably catch what already
happened.

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-CF-001 | D1 | security/ops | The deploy step would have been gated | Classify the incident's deploy command using only the protocol's table | It lands in the deploy tier and is forbidden ad hoc, requiring an explicit confirmation naming the target before it could proceed | PASS - the deploy operation lands in the deploy tier, Forbidden ad hoc, requiring a written proposal and human approval naming the target. | CONFIRMED. | PENDING |
| AC-CF-002 | D1 | security/ops | The credential step is caught on the credential axis alone | Classify the incident's read command using only the protocol's table, ignoring the mutation axis | It is forbidden by the credential-flow rule purely because a secret entered the argument string, independent of being a read. If the protocol only stops it via the mutation axis, this AC FAILS and the classification design needs rework | PASS - S2.4 forbids an argv-borne secret at every mutation tier including read, explicitly overriding the mutation axis rather than being weighed against it. | CONFIRMED - verified independently by reading S2.4. It states a read leaking a credential is worse than a write that leaks nothing, which is exactly the invariant this row exists to test. | PENDING |
| AC-CF-003 | D1 | security/ops | Protocol is usable by someone without incident context | Have a reader with no background classify three commands not named anywhere in the document | All three are classified correctly using only the table and rules, demonstrating a general protocol rather than a narrative of one incident | PASS - novel commands classified correctly from the table alone. | CONFIRMED. | PENDING |

## Compliance Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-SEC-001 | D1, D5, D6, D7 | security/ops | Public-Repo Rule compliance | Search every artifact for the live service name, its domain, the private profile repo name, client names, and internal tooling or workflow names | None appears in any committed artifact; the incident is described generically throughout. This is the most likely way this work ships broken, so it is checked by search, not by recollection | (not delegated - orchestrator-owned) | PASS - orchestrator-run search: no live service name, domain, private profile repo, client name, or internal tooling/skill name appears in any artifact produced by this work. Verified against added lines only (git diff) and against the new files directly; pre-existing occurrences elsewhere in the repo predate this work and were not introduced by it. | PENDING |
| AC-SEC-002 | D1 | security/ops | No credential value is reproduced | Search the protocol for any real secret, token, or password value | The document names credentials only by variable name and never reproduces a value, including in the incident annex's example commands | PASS - credentials referenced by variable name only; no value reproduced, including in the incident annex. | CONFIRMED. | PENDING |
| AC-DOC-001 | D1 | security/ops | Citations are verified, not remembered | Re-read every cited file and line reference at verification time | Every file-and-line citation in the protocol resolves to code that actually supports the claim made about it. A prior plan in this repo recorded an AC built from prose that proved wrong once checked against reality; this AC exists because of that | FAIL - 27 of 28 citations verified, but S2.5 row 1 (`read | neither`, the row a reader lands on for 'safe, allowed ad hoc') cited getStatus(), which sends Basic auth on every call via authHeaders. | CONFIRMED-AND-FIXED - verified independently: getStatus -> getJson -> fetchImpl(..., {headers: authHeaders}). Real defect on the most consequential row. Row 1 now cites waitForHealthy against the documented-unauthenticated /setup/healthz, and names getStatus explicitly as a near miss belonging to the requires-a-secret row. | PENDING |
