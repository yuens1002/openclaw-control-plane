# Onboarding Regression Pipeline ACs

Source plan: `docs/plans/onboarding-regression-pipeline/plan.md`.

Pass conditions are invariants over behavior (call sequence, call presence,
error-propagation shape), never over a live OpenRouter/Railway state or a
specific secret value.

| AC | Plan ref | Role | Pass invariant | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- |
| AC-KEY-001 | D2 | devops | `mintOpenRouterKey` returns `{key, hash}`; throws a clear error (naming the missing field) if either is absent from the response, rather than returning `undefined`/`""` silently. | PASS - `openrouter-provisioning.ts:44-73`; throws naming `hash` specifically when absent. | PASS - covered by "throws if the response has a key but no hash" test. | |
| AC-KEY-002 | D2 | devops | `deleteOpenRouterKey(hash, options)` issues `DELETE https://openrouter.ai/api/v1/keys/{hash}` with `Authorization: Bearer <managementKey>`, throws `OpenRouterProvisioningError` on a non-2xx response, and never includes the response body in a thrown error. | PASS - `openrouter-provisioning.ts:75-87`. | PASS - "DELETEs /api/v1/keys/{hash}..." and "throws a status-only error..." tests in `openclaw-setup-applier-openrouter-provisioning.test.ts`. | |
| AC-KEY-003 | D2 | devops | `applyProfile`'s `ApplyResult` includes `mintedKeyHash` set to the minted key's hash when `resolveProviderSecret` actually minted (the secret name was absent from Railway variables), and is `undefined` when the secret already existed (no mint occurred). | PASS - `apply-profile.ts:245-265,307`. | PASS - "mints, writes with --skip-deploys..." asserts `mintedKeyHash` set; new "leaves mintedKeyHash undefined when no mint occurred" test asserts the negative case. | |
| AC-CYC-001 | D3 | devops | `bootstrapOnboardingCycle` calls `provisionClientInstance` then `dryRunApplyProfile` then `applyProfile`, in that order, and returns `applyProfile`'s result (including `mintedKeyHash`) without calling `deleteOpenRouterKey` itself. | PASS - `onboarding-cycle.ts:46-73`. | PASS - "provisions, dry-runs, then applies, in that order, and returns mintedKeyHash" asserts call order via `calls` array and `expect(calls).not.toContain("delete")`. | |
| AC-CYC-002 | D3 | devops | `runRegressionCheck` mints a key, writes it to the profile's declared model-provider secret name via `writeRailwayVariable`, then checks `/setup/api/status` (`configured: true`) and `/setup/healthz` (200). | PASS - `onboarding-cycle.ts:143-176`. | PASS - "mints a fresh key, writes it, verifies status+healthz, and deletes the key on success" asserts call order `["mint","status","healthz","delete"]` and the write args. | |
| AC-CYC-003 | D3 | devops | **Guaranteed deletion.** When the status/healthz checks inside `runRegressionCheck` throw (simulated via an injected dependency that rejects), `deleteOpenRouterKey` is still called exactly once with the freshly-minted hash before the function's own error propagates. Deletion is asserted via a call-count spy, not inferred from the absence of a thrown secondary error. | PASS - `onboarding-cycle.ts:158-183` (`try`/`catch`/`finally`, delete in `finally`). | PASS - two tests: "guarantees deletion even when the status check throws" and "guarantees deletion even when the Railway variable write itself throws" (the latter throws before any HTTP call, proving the `finally` covers non-fetch failures too), both spy on delete-call count. | |
| AC-CYC-004 | D3 | devops | When the status/healthz checks in `runRegressionCheck` all pass, `deleteOpenRouterKey` is still called exactly once (delete-every-run, not delete-only-on-failure). | PASS - same `finally` block, unconditional on `passed`. | PASS - "mints a fresh key, writes it, verifies status+healthz, and deletes the key on success" asserts exactly one `"delete"` entry in `calls` on the success path. | |
| AC-CLI-001 | D4 | devops | `onboarding-cycle-cli.ts` exposes `bootstrap`, `regression-check`, and `delete-key` subcommands; each validates its required flags/env vars and throws a named-flag error (matching `cli.ts`'s `requireValue`/`requireEnv` pattern) rather than a generic parse failure. | PASS - `onboarding-cycle-cli.ts:15-79` (subcommand dispatch), `86-215` (parsers), `217-224` (`requireValue`), `226-231` (`requireEnv`). | PASS - `openclaw-setup-applier-onboarding-cycle-cli.test.ts`, 9 tests covering all three parsers' required-flag and unknown-flag errors. | |
| AC-CLI-002 | D4 | devops | The CLI's `runCommand`/`fetch` wiring never writes a captured stdout/response body to this process's own stdout — same discipline as `cli.ts`'s existing `runCommand` (secrets can appear in `railway variable` output). | PASS - `onboarding-cycle-cli.ts:244-247` (stdout captured, never `process.stdout.write`'d). | PASS - "never writes the spawned process's stdout to this process's own stdout" test, real subprocess (no fake runner), matching `client-cli.test.ts`'s precedent exactly. | |
| AC-TEST-001 | D5 | test-engineer | Mocked tests exist and pass for AC-KEY-001–003 and AC-CYC-001–004 above, each against injected fetch/dependency stubs — no live OpenRouter or Railway call in any test. | PASS - all three new/extended test files use injected `fetchImpl`/`FakeRailwayRunner`; no call reaches `openrouter.ai` or a real `railway` binary except the one deliberate real-subprocess `runCommand` test (which spawns `node`, not `railway`, matching `client-cli.test.ts`'s own precedent). | PASS - full suite `npm test`: 23 files / 147 tests green; `tsc -b` clean. | |
| AC-DOCS-001 | D6 | project-manager | README/deploy docs describe the `onboarding-cycle` CLI's three subcommands and explicitly state the dead-key-between-runs tradeoff (the fixture's model-provider secret holds a deleted key's value until the next `regression-check` overwrites it). | PASS - `deploy/openclaw-railway/README.md`'s new "Onboarding Regression Pipeline" section. | PASS - confirmed by reading the section; states the tradeoff explicitly as "Accepted tradeoff, not a bug." | |

## Gate 1 — Plan ↔ ACs coverage (manual check, no repo validator script exists)

Every deliverable D2–D6 has at least one AC row above (D1 is this pair of
docs and is self-covering). No AC row references a deliverable ID outside
D1–D6.

## Gate 2 — Anti-drift check (manual, no repo lint script exists)

No Pass-invariant cell above pins a literal secret value, hash string, or
domain — each cell states behavior over the injected stub's captured calls.
