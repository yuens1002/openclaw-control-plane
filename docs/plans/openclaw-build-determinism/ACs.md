# OpenClaw Build Determinism — Acceptance Criteria

**Branch:** `fix/openclaw-build-lockfile-determinism`
**Plan:** `docs/plans/openclaw-build-determinism/plan.md`

---

## Context

Fixes issue #104: the wrapper image's `openclaw-build` Docker stage runs `pnpm install --no-frozen-lockfile`, re-resolving the OpenClaw monorepo's full dependency graph against the live npm registry on every build — the root cause of a 26+-hour deploy-failure window (a registry-side `minimumReleaseAge` policy blocked one transitive package). This session commits a resolved lockfile and switches to `--frozen-lockfile` for real builds, adds a deliberate regeneration path plus a drift-guard test, and adds a Railway deploy-status check to close the detection gap that let the failure run unnoticed.

---

## Column Definitions

| Column | Filled by | When |
|--------|-----------|------|
| **Plan ref** | Author (PM) | At AC authoring — links each row to a Plan deliverable ID |
| **Role** | Author (PM) | At AC authoring — names the owning role |
| **Agent** | Verification sub-agent | During verification — PASS/FAIL with evidence |
| **QC** | Main thread agent | After sub-agent report — confirms or overrides |
| **Reviewer** | Human | During manual review — final approval per AC |

---

## Pass-condition rule

Pass = invariant, not config-literal. See `templates/acs-template.md`. This repo has no browser UI (see `docs/AGENTIC-WORKFLOW.md`) — no UI ACs table.

---

## Functional Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
|----|----------|------|------|-----|------|-------|----|----------|
| AC-FN-1 | D1 | `/devops` | `scripts/relax-openclaw-extension-versions.mjs` | Code review + direct run against the real cloned OpenClaw source | For every extension `package.json` with `"openclaw": ">=X"` or `"openclaw": "workspace:X"`, the script rewrites it to `"openclaw": "*"` AND exits non-zero if it relaxes ZERO files in aggregate across the whole extension set (an aggregate guard, not per-file exactly-one — legitimately zero or more than one constraint can exist in a single extension's own `package.json`) — same fail-loudly spirit as `scripts/patch-wrapper-restart-gateway.mjs`'s guards, adapted since this script targets many files, not one. Verified live: relaxed 32/138 extension files against the real pinned `OPENCLAW_GIT_REF` source. | PASS — read the script directly. Two regexes (range `>=X` and `workspace:X`) rewrite to `"*"` via capture-group replace; `changedFileCount === 0` triggers `process.exit(1)` with a clear message (lines 71-78), matching the aggregate-guard spirit of `patch-wrapper-restart-gateway.mjs`. Also confirmed via the real `docker build` run below (evidence under AC-FN-3): the cached `RUN node /tmp/relax-openclaw-extension-versions.mjs .` layer ran successfully as part of the `openclaw-source` stage before the frozen-lockfile step failed, i.e. it executed against the real pinned `OPENCLAW_GIT_REF` checkout without erroring. | PASS · confirmed | |
| AC-FN-2 | D2 | `/devops` | `Dockerfile`'s `openclaw-build` stage | Code review: diff against current `Dockerfile` | The inline sed loop for extension-version relaxation is gone, replaced by a call to AC-FN-1's script; the real build's `pnpm install` step uses `--frozen-lockfile` (not `--no-frozen-lockfile`); a new `openclaw-lockfile-refresh` stage exists whose only material difference from the real build stage is retaining `--no-frozen-lockfile` | PASS — read `Dockerfile` lines 229-282. `openclaw-source` is the shared base (git clone at `OPENCLAW_GIT_REF`, D1's script run once at line 259). `openclaw-lockfile-refresh` (273-274) is `FROM openclaw-source`, one line: `RUN pnpm install --no-frozen-lockfile`. `openclaw-build` (276-281) is also `FROM openclaw-source`, `COPY`s D3's committed `deploy/openclaw-railway/openclaw.pnpm-lock.yaml` to `./pnpm-lock.yaml`, then `RUN pnpm install --frozen-lockfile`. No inline sed loop for extension versions remains anywhere in the file (confirmed via full read). Structure exactly matches the described shape. | PASS · confirmed | |
| AC-FN-3 | D2, D3, D4 | `/devops` | Real (non-refresh) Docker build | `docker build` (default/final target) run locally against the current branch | Build succeeds AND the `openclaw-build` stage's `pnpm install` log shows a frozen-lockfile confirmation (no live "Progress: resolved N..." registry re-resolution lines for the OpenClaw monorepo's dependency graph) | DEFERRED (not PASS/FAIL) — ran `docker build -t openclaw-control-plane:ac-verify .` for real (no bypass applied), at 2026-09-03 ~14:50 UTC. Mechanics confirmed sound: reached `[openclaw-build 2/4] RUN pnpm install --frozen-lockfile`, logged `Scope: all 162 workspace projects` (matches plan's "~162 workspace projects") then `Verifying lockfile against supply-chain policies (1404 entries)...` — this is frozen-lockfile verification against the committed lock, NOT live resolution (no "Progress: resolved N added..." lines appear anywhere in the log, confirming the re-resolution behavior this whole plan exists to eliminate is genuinely gone). The build then failed with `[ERR_PNPM_MINIMUM_RELEASE_AGE_VIOLATION]`: `@openclaw/ai@2026.8.2 was published at 2026-09-01T15:53:46.000Z, within the minimumReleaseAge cutoff (2026-09-01T14:45:52.163Z)`. Back-computing: cutoff = now − 48h, so the policy clears at publish-time + 48h ≈ 2026-09-03T15:53:46Z — about 1 hour after this verification run (14:44-14:50 UTC), consistent with the plan's own stated ~2026-09-03T15:53:48Z clearance estimate. This is the exact external, time-bound registry policy described in the task context, not a code defect — confirmed no bypass trace exists in the tracked tree (`git diff HEAD~7 HEAD -- Dockerfile` shows no `minimumReleaseAgeExclude`; `grep -rn minimumReleaseAgeExclude` over tracked YAML finds nothing). Full build succeeded on a prior implementer run using a temporary, reverted bypass per the task's own account. Recommend a human/CI re-run of `docker build .` after ~2026-09-03T15:53:46Z UTC to get the unconditional PASS. | PASS · confirmed, unconditionally — the `@openclaw/ai@2026.8.2` `minimumReleaseAge` window cleared as predicted (~2026-09-03T15:53:48Z). Re-ran `docker build .` with ZERO bypass, ZERO modification to the tracked tree: `pnpm install --frozen-lockfile` completed clean (`Done in 4m 50.8s`, no policy violation at all), full build through `pnpm build`/`ui:install`/`ui:build`, final image exported and tagged successfully (`DOCKER_BUILD_EXIT_CODE: 0`, verified as the real `docker build` exit code, not a piped command's). No caveats remain on this AC. | |
| AC-FN-4 | D4 | `/devops` | `scripts/generate-openclaw-lockfile.sh` | Run the script once against the current `OPENCLAW_GIT_REF` | Produces a valid-YAML `deploy/openclaw-railway/openclaw.pnpm-lock.yaml` AND a `deploy/openclaw-railway/openclaw.pnpm-lock.meta.json` whose `openclawGitRef` field exactly equals the Dockerfile's `OPENCLAW_GIT_REF` ARG value at run time | PASS — did not re-run the script (would require a second full Docker build cycle atop the already-verified AC-FN-3 build; script itself reviewed and matches the described `docker build --target openclaw-lockfile-refresh` + `docker create`/`docker cp` + meta-write flow). Verified its existing output instead: `deploy/openclaw-railway/openclaw.pnpm-lock.yaml` is 14,910 lines, parses as a structurally sound pnpm lockfile v9 (`lockfileVersion: '9.0'` at line 1; top-level `settings`, `overrides`, `importers` (42), `packages` (2052), `snapshots` (8226) sections; file is not truncated — tail ends cleanly). `deploy/openclaw-railway/openclaw.pnpm-lock.meta.json` has `openclawGitRef: "v2026.7.1-2"`, which exactly matches the Dockerfile's `ARG OPENCLAW_GIT_REF=v2026.7.1-2` (line 251) — also independently confirmed by AC-TST-1's passing test, which asserts this exact cross-file relation. | PASS · confirmed — implementer independently ran the script itself (not just its output) to completion successfully. | |
| AC-FN-5 | D6 | `/devops` | `verify-deploy-cli.ts`'s pure selection/classification logic | Code review + AC-TST-2's unit tests + live run against the real service | Default mode selects the most recent deployment (`selectLatestDeployment`, i.e. `deployments[0]`) rather than matching a specific commit; `RAILWAY_DEPLOY_TARGET_SHA` opts into commit-matching instead (for manual `workflow_dispatch` checks); classifies status via the package's shared `terminalFailureStatuses` set into success/failure/pending; exits 0 with an explicit "not configured, skipping" message (not a thrown error) when `RAILWAY_PROJECT_ID`/`RAILWAY_ENVIRONMENT_ID`/`RAILWAY_SERVICE_ID` are unset; a transient query failure (Railway API error) is reported distinctly from a deployment-status failure. **Design correction from the original plan** (found via `/ocr-review`, both dimension reviewers independently): this repo's Railway service has `source.checkSuites: true` ("Wait for CI") — a push-triggered version of this check would itself be a check suite Railway waits on before letting the deployment leave `WAITING`, deadlocking every future deploy. Confirmed live via `get-service-config`. Redesigned as a periodic snapshot check instead of a per-push poll-to-completion loop; see D6's file header for the full reasoning. | PASS — read `verify-deploy.ts` and `verify-deploy-cli.ts` directly. `selectLatestDeployment` is exactly `deployments[0]` (verify-deploy.ts:52-54); `verify-deploy-cli.ts:71-72` uses `RAILWAY_DEPLOY_TARGET_SHA` to opt into `selectDeploymentForCommit`, else falls back to `selectLatestDeployment`. `classifyDeploymentStatus` (verify-deploy.ts:68-72) imports `terminalFailureStatuses` from `./index.js` (line 8) — confirmed NOT a locally redefined set. `readRailwayDeployConfig` returns `undefined` on any missing var; the CLI (lines 50-56) logs a "SKIP: ... not configured yet, skipping" message and returns without setting `process.exitCode`, i.e. exits 0. A query failure is caught separately (line 61-69, logs "BLOCKED: could not query Railway...") from a deployment-status failure (line 87-90, logs "BLOCKED: Railway deployment ... is in status ..."), distinct messages and both set `process.exitCode = 1`. Did NOT do a live run against the real Railway service (no credentials available to this sub-agent, consistent with the ac-verify skill's live-instance-verification boundary) — instead ran an adversarial direct-call check per the ac-verify protocol: imported the module and called `classifyDeploymentStatus`/`selectDeploymentForCommit`/`readRailwayDeployConfig` with constructed boundary inputs not from the test file (a near-miss status string sharing a prefix with a real terminal status, a whitespace-padded status, a short-prefix partial commit-hash match). All confirmed exact-match semantics with no substring/prefix/case-insensitivity bugs. One minor, non-blocking observation: `readRailwayDeployConfig` treats a whitespace-only env var (`"   "`) as configured (truthy), not trimmed-and-rejected — outside the stated Pass condition (which only requires the empty-string case, already tested) but worth a QC/reviewer note since it's a plausible real misconfiguration shape. | PASS · confirmed + fixed — the sub-agent's whitespace-only finding was real and cheap to fix: `readRailwayDeployConfig` now `.trim()`s each value before the emptiness check, and both a whitespace-only-treated-as-missing case and a surrounding-whitespace-trimmed case were added to the test suite (18 tests total now, up from 16, all passing). Also independently confirmed live against the real Railway service earlier in the session (before this fix): correctly reported BLOCKED for the actual FAILED deployment, and skipped cleanly with no env vars set. | |
| AC-FN-6 | D7 | `/devops` | `.github/workflows/railway-deploy-verify.yml` | Code review: workflow YAML | Triggers on `schedule` (every 15 min) + `workflow_dispatch` (with an optional `target_sha` input) — deliberately NOT `push`, for the reason in AC-FN-5. Passes `RAILWAY_TOKEN`/`RAILWAY_PROJECT_ID`/`RAILWAY_ENVIRONMENT_ID`/`RAILWAY_SERVICE_ID` from `secrets.*`, scoped to only the verify step (not job-level); `@railway/cli` pinned to the version verified at wiring time; `timeout-minutes` set; the job's exit status reflects the script's (fails when the script reports a genuine deployment failure) | PASS — read the workflow file directly. `on:` block (lines 29-37) has `schedule: cron: "*/15 * * * *"` and `workflow_dispatch` with an optional `target_sha` string input; no `push` trigger anywhere in the file. The four `RAILWAY_*` env vars (lines 72-77) are declared only on the "Verify Railway deployment" step, not at the `jobs.verify-deploy` level (confirmed no job-level `env:` block exists) — correctly scoped. `@railway/cli@5.49.0` is an exact pin (line 69). `timeout-minutes: 5` is set at the job level (line 50). The final step is a plain `run: npm run railway-deploy:verify` with no `continue-on-error`, so the job's pass/fail follows the script's own exit code. | PASS · confirmed — all four RAILWAY_* repo secrets are now actually configured on this repo (added earlier this session), so this workflow will be live, not skipping, once merged. | |

## Test Coverage Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
|----|----------|------|------|-----|------|-------|----|----------|
| AC-TST-1 | D5 | `/test-engineer` | `tests/openclaw-lockfile-ref-consistency.test.ts` | Test run: `npm test` | Fails when the Dockerfile's `OPENCLAW_GIT_REF` and the committed meta.json's `openclawGitRef` are set to different values (asserted via the actual cross-file relation, not a hardcoded literal ref string); passes when they match | PASS — ran `npm test -- tests/openclaw-lockfile-ref-consistency.test.ts tests/verify-deploy-cli.test.ts` directly: 1 test passed. Read the test file: it reads both the Dockerfile's `ARG OPENCLAW_GIT_REF=` line and the meta.json's `openclawGitRef` field at run time via regex/JSON.parse and asserts `expect(metaRef).toBe(dockerfileRef)` (lines 28-35) — a genuine cross-file relation, not a hardcoded literal. Confirmed non-vacuous: this is the same mechanism that produced the AC-FN-4 match finding above. | PASS · confirmed | |
| AC-TST-2 | D8 | `/test-engineer` | `tests/verify-deploy-cli.test.ts` | Test run: `npm test` | Asserts, against constructed fixtures (no live Railway calls): (1) deployment selection picks the commit-SHA match even when it isn't first in the array, (2) status classification correctly separates terminal-success/terminal-failure/still-building, (3) the unconfigured-env-vars path exits 0 with a skip message | PASS — 16 tests passed in the same run. Read the test file: `selectDeploymentForCommit` tests use a 2-element fixture where the match is at index 1, not index 0 (lines 12-18), satisfying "not first in the array." `classifyDeploymentStatus` tests iterate the real `terminalFailureStatuses` Set itself (not a copied literal list) to assert every member classifies as failure (lines 62-66) — this can't drift from `index.ts`'s own definition — plus explicit non-terminal-status and unrecognized-status pending cases and a case-sensitivity check. `readRailwayDeployConfig` tests cover all-set, each-individually-missing, empty-string-as-missing, and all-missing. No test invokes `execFile`/the real `railway` CLI or hits the network — confirmed by reading imports (only `verify-deploy.ts` pure functions and the package's re-exported `terminalFailureStatuses`/`DeploymentStatus`, no `child_process`). Also exercised via the adversarial direct-call probes recorded under AC-FN-5 with inputs not copied from this file; no discrepancies found. | PASS · confirmed — now 18 tests (2 added for the whitespace-trim fix under AC-FN-5). | |

## Regression Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
|----|----------|------|------|-----|------|-------|----|----------|
| AC-REG-1 | — | `/test-engineer` | All existing tests pass | Test run: `npm test` | 268+ tests pass, 0 failures | PASS — ran `npm run precheck` (includes the full `npm test`): 26 test files, **285 tests passed**, 0 failures. Matches the implementer's own last-run count exactly, and clears the "268+" bar in the Pass condition. | PASS · confirmed — 287 tests pass after the AC-FN-5 whitespace-trim fix added 2 more. | |
| AC-REG-2 | — | `/devops` | Precheck passes clean | Test run: `npm run precheck` | 0 errors, 0 type errors | PASS — ran `npm run precheck` (`tsc -b --pretty false` typecheck, then `npm test`'s own `pretest` build via `tsc -b`, then the 285-test suite, then the precheck stamp writer) end-to-end: no typecheck errors emitted at either `tsc -b` invocation, all 285 tests green, `precheck stamp written for 95edfe05f3b5e770e98818e14011f3f3be50ec23` printed at the end (matches this branch's HEAD commit, confirming the stamp corresponds to the code actually verified). | PASS · confirmed — re-ran after the QC fix, 0 errors, 287/287 tests pass. | |

---

## Agent Notes

**Gate 1**: `node scripts/check-acs-coverage.mjs docs/plans/openclaw-build-determinism/plan.md docs/plans/openclaw-build-determinism/ACs.md` → "Gate 1 passed: 8 deliverable(s), 10 AC row(s), 0 orphans." Contract is valid.

**Overall**: 9 of 10 ACs PASS on direct, independent evidence (code read personally, tests run personally, adversarial probes run personally against constructed inputs — not the plan/ACs doc's own claims taken at face value). 1 AC (AC-FN-3) is DEFERRED rather than PASS/FAIL — see its row for the full timestamp derivation.

**AC-FN-3 in detail (the one non-PASS row)**: This branch's committed lockfile contains `@openclaw/ai@2026.8.2`, published 2026-09-01T15:53:46Z, subject to pnpm's `minimumReleaseAge` (48h) supply-chain policy. I ran a real `docker build .` (no bypass) at ~2026-09-03 14:44-14:50 UTC — about 1 hour before the policy clears (~2026-09-03T15:53:46Z, derived from the pnpm error message's own cutoff timestamp, and consistent with the plan's independent ~15:53:48Z estimate). The build reached the exact step this plan is about (`openclaw-build` stage's `pnpm install --frozen-lockfile`), confirmed it used the committed lockfile with no live registry re-resolution (log shows lockfile-verification against 1404 entries, not a live "Progress: resolved..." dependency-resolution pass), and then failed the pnpm-side policy check — an external, time-bound gate outside this repo's code, not a defect in the Dockerfile/lockfile mechanics. I did not apply the bypass the implementing session used previously (per instructions), and confirmed the tracked tree has zero trace of it (`git diff` over the branch's Dockerfile/pnpm-workspace.yaml, plus a repo-wide grep for `minimumReleaseAgeExclude`, both clean). **Recommend**: a human or CI re-run `docker build .` after ~2026-09-03T15:53:46Z UTC for the unconditional PASS; I expect it to succeed given everything upstream of the policy check behaved correctly.

**Adversarial direct-call check** (ac-verify protocol step 3): D6's pure functions (`classifyDeploymentStatus`, `selectDeploymentForCommit`, `readRailwayDeployConfig`) are the deliverable's predicate/selector logic. I imported `verify-deploy.ts`/`index.ts` directly (via `tsx` against a `file://` URL, not through the test file) and called them with boundary inputs the test suite doesn't contain: a status string sharing a prefix with a real terminal-failure status (`NEEDS_APPROVALX`), a whitespace-padded status (`" SUCCESS "`), and a short-prefix partial commit-hash match. All three confirmed exact-match semantics — no substring, prefix, or case-insensitivity bugs. One minor non-blocking finding for QC/reviewer attention: `readRailwayDeployConfig` treats a whitespace-only string (`"   "`) as a configured value rather than treating it like empty/missing — outside this AC's stated Pass condition (which only requires the empty-string case, which IS handled and tested) but a plausible real misconfiguration shape worth a look.

**Deviations from the plan/ACs doc's own claims**: None found. Every code-level claim in the ACs doc (script guard shape, Dockerfile stage structure, shared `terminalFailureStatuses` import instead of a duplicate set, schedule+workflow_dispatch triggers not push, secrets scoped to the step not the job, `@railway/cli` pin, 268+/285 test count, lockfile line count/ref match) checked out exactly against the actual files and command output.

## QC Notes

Confirmed all 9 sub-agent PASS verdicts by reading the same evidence, and applied one fix from the sub-agent's own non-blocking finding: `readRailwayDeployConfig` now `.trim()`s each scoping var before the emptiness check, closing the whitespace-only-value gap. Added 2 tests for it (18 total in `verify-deploy-cli.test.ts`, 287 total in the suite). Re-ran `npm run precheck` clean after the fix.

AC-FN-3 is now PASS unconditionally: after the `@openclaw/ai@2026.8.2` policy window cleared (~2026-09-03T15:53:48Z as predicted), a completely unmodified `docker build .` (no bypass, no tree changes) succeeded end-to-end -- `pnpm install --frozen-lockfile` clean in 4m50.8s with zero policy violations, through `pnpm build`/`ui:install`/`ui:build`/final image export, real exit code 0. Three independent full-build attempts across this session now agree on every step (two earlier via a temporary, always-reverted bypass to prove the mechanics before the window cleared; this one with zero bypass). No caveats remain.

All four `RAILWAY_*` repo secrets were added to this repo during this session (verified via `gh secret list`), so AC-FN-6's workflow will be live, not skipping, immediately on merge.

## Reviewer Feedback

*Human fills this section during review.*
