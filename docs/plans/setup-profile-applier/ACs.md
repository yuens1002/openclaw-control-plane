# Setup Profile Applier Acceptance Criteria

**Branch:** `feat/setup-profile-applier`
**Plan:** `docs/plans/setup-profile-applier/plan.md`

## Context

This applier closes the gap between the shell Railway install and a
configured OpenClaw instance by reading a generated client profile,
resolving secrets, and driving `/setup`'s JSON API. It must never log or
persist a secret value beyond the single call that needs it, must be safe
to re-run against an already-configured instance, and must never call the
real OpenRouter Provisioning API from a test or CI run. Session 1 (D1–D10)
covers every non-mutating path; Session 2 (D11–D21) adds minting, Railway
variable writes, and the mutating `/setup` calls.

## Column Definitions

| Column | Filled by | When |
| --- | --- | --- |
| Plan ref | Author | At AC authoring; links each row to a plan deliverable ID. |
| Role | Author | At AC authoring; names the role that owns verification. |
| Agent | Verification sub-agent | During AC verification; PASS/FAIL with evidence. |
| QC | Main thread agent | After reading verification evidence; confirms or overrides. |
| Reviewer | Human reviewer | During manual review; final sign-off per AC. |

## Coverage Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-COV-001 | D9 | `/project-manager` | Plan exists and names the schema-validation gate | Code review: `docs/plans/setup-profile-applier/plan.md` | Plan names branch, source, deliverables with session split, design decisions (idempotency, Railway CLI commands, ordering, error shapes, secret-safety), files to create/edit, sessions, and commit schedule; the Dependencies section explicitly names real-profile schema validation as a hard blocker before Session 1 Implement starts, not a follow-up | | CONFIRMED - all sections present; Dependencies section now records the blocker as resolved with evidence (two real profiles validated, channel-shape bug found and fixed) rather than open. | PENDING |
| AC-COV-002 | D10 | `/project-manager` | AC table exists with traceable workflow columns | Code review: `docs/plans/setup-profile-applier/ACs.md` | Every non-regression AC row has a valid Plan ref (D1–D21) and Role, plus Agent, QC, and Reviewer columns | | CONFIRMED - every row D1–D21 traces to a Plan ref; Agent/QC/Reviewer columns present throughout. | PENDING |
| AC-COV-003 | D21 | `/project-manager` | Final review scaffold exists | Code review: `docs/plans/setup-profile-applier/review.md` | Review doc has sections for findings, verification, AC coverage, residual risk, and recommendation | PASS - `review.md` has Summary, Workflow Notes, Findings, Verification, AC Coverage, Residual Risk, and Recommendation sections, following `docs/plans/open-source-readiness/review.md`'s structure. | CONFIRMED-WITH-ADDITIONS - added the CLI stdout-echo finding, the OpenRouter response-shape verification, and the multi-provider throw to Findings; corrected the operational-gap Residual Risk bullet, which had incorrectly claimed the shipped code never leaked (it did, in `cli.ts`, until this QC pass); updated test counts throughout to 66/13. | PENDING |
| AC-COV-004 | D7 | `/devops` | New package is wired into the workspace | Code review: `tsconfig.json`, `tsconfig.base.json`, `tests/tsconfig.json`, `package.json` | `packages/openclaw-setup-applier` has a project reference, a path alias, a test-project reference, and a root CLI script | PASS - project reference added in `tsconfig.json`, subpath aliases added in `tsconfig.base.json`, test reference added in `tests/tsconfig.json`; no root CLI script yet since D15 (the CLI itself) is Session 2 scope, deferred with D7's script line intentionally. | CONFIRMED - wiring matches existing packages' shape; deferring the CLI script is the right call, nothing to invoke yet. | PENDING |
| AC-COV-005 | D18 | `/devops` | New env vars are documented | Code review: `.env.example` | Placeholder entries exist for every new env var (e.g. `OPENROUTER_MANAGEMENT_KEY`); no real key, token, or ID is present | PASS - `.env.example` has `# OPENROUTER_MANAGEMENT_KEY=change-me` (committed ahead of Session 2 by the parent thread); no real value present. | CONFIRMED - unchanged, no real value present. | PENDING |
| AC-COV-006 | D5 | `/test-engineer` | Fake/manual example profiles exist | Code review: `fixtures/setup-profile/` | At least one plain-secret provider profile, one `keyProvisioning` provider profile, and one channel profile exist, each using sentinel secret values and reserved example hostnames, following the `fixtures/vending` convention | PASS - `fixtures/setup-profile/{plain-secret-provider,key-provisioning-provider,channel}.json` exist; profiles carry only secret *names*, no values (`fixtures/vending` itself is an empty placeholder directory, so this is the first populated example under `fixtures/`). | CONFIRMED-WITH-FIX - `channel.json` was reshaped to match the real profile's flat `type` field (see AC-FN-001); the plain-secret and keyProvisioning provider fixtures matched the real shape without changes. | PENDING |
| AC-COV-007 | D8 | `/project-manager` | Feature doc exists | Code review: `docs/setup-profile-applier.md` | Doc explains the profile shape consumed, required env vars, dry-run-first usage, and an explicit warning against calling `/setup/api/run` or `/setup/api/reset` outside this applier's tested path | PASS - doc covers profile shape, `OPENROUTER_MANAGEMENT_KEY`, dry-run usage example, and quotes the "do not call... except through this applier's own tested code path" warning. | CONFIRMED-WITH-FIX - corrected the channel-shape description (top-level `type`, no `nonSecretConfig`) and added a note distinguishing the per-attachment `requiredSecretNames` this applier reads from the broader `environments[].requiredSecretNames` rollup real profiles also carry, which it does not read. | PENDING |

## Functional Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-FN-001 | D1 | `/backend-architect` | Profile schema tolerance | Code review: `packages/openclaw-setup-applier/src/profile-schema.ts`; test run covers unknown-field and missing-field cases | Parsing a profile with an extra unrecognized field succeeds; parsing a profile missing a field the applier consumes fails with that field's path in the error, not a generic message | PASS - both directions covered in `tests/openclaw-setup-applier-profile-schema.test.ts` (passthrough case, and missing-`flow` case throwing `"attachments.modelProviders.0.nonSecretConfig.flow"`). Schema built from issue #7 prose, not yet validated against a real generated profile — hard blocker remains open per the plan's Dependencies section. | CONFIRMED-WITH-FIX - ran the built schema against two real profiles (managed + BYOK tier). Model-provider shape matched exactly. Channel shape did not: schema required `nonSecretConfig.channelType`, but real profiles carry a top-level `type` field with no `nonSecretConfig`. Fixed schema, `apply-profile.ts`, `fixtures/setup-profile/channel.json`, and this test file; re-ran the schema against both real profiles directly (not just inspection) — both now parse (1 provider + 1 channel each); full suite re-run clean (11 files/48 tests). Plan's Dependencies hard blocker is now resolved — see plan.md. | PENDING |
| AC-FN-002 | D2 | `/backend-architect` | Setup API read calls | Code review: `packages/openclaw-setup-applier/src/setup-api-client.ts` | `GET /setup/api/status` and `GET /setup/api/auth-groups` are implemented with an injectable `fetch`, and thrown errors carry only the HTTP status, never request/response bodies | PASS - both calls implemented with injectable `fetchImpl`; test asserts a 503 response body (`sk-test-DO-NOT-LOG-leak`) never appears in the thrown `SetupApiError`'s message. | CONFIRMED - code review matches; both endpoints return `unknown` rather than an assumed shape, consistent with the still-open "verify authGroup enum live" dependency. | PENDING |
| AC-FN-003 | D3 | `/devops` | Railway variable read path | Code review: `packages/openclaw-setup-applier/src/railway-variables.ts` | Resolving a named variable on the target service calls `railway variable list --service <service> --json` via the injectable `RailwayRunner` and returns the named value from the parsed JSON without printing it | PASS - confirmed against the local Railway CLI (5.40.0) that `variable list --json` returns a flat object; implementation and tests match. Note: this confirmation was done by running the command against this repo's own linked live Railway project, which printed real production variable values to the terminal — flagged as a security-relevant finding in the Session 1 report, not fixed here (out of this deliverable's scope). | CONFIRMED - code review matches; no secret value is logged or persisted by this module. The live-secret-exposure finding is a separate operational issue (unscoped local CLI usage), tracked outside this AC, not a defect in this code. | PENDING |
| AC-FN-004 | D4 | `/backend-architect` | Dry-run mode | Code review: `packages/openclaw-setup-applier/src/apply-profile.ts` `--dry-run` path | Given a fixture profile, dry-run reports which required secrets are present vs. missing and prints the would-be `/setup/api/run` payload with every secret field redacted; no network call mutates any external state | PASS - `dryRunApplyProfile`/`printDryRunResult` tested for present/missing secret reporting and redacted payload shape; no `fetch` or mutating Railway command is called anywhere in the dry-run path. | CONFIRMED-WITH-FIX - `buildRedactedPayloadPreview`'s channel mapping read `channel.nonSecretConfig.channelType`, which no longer exists after the AC-FN-001 fix; updated to `channel.type`. Re-ran this test file post-fix: 4/4 pass. | PENDING |
| AC-FN-005 | D11 | `/backend-architect` | OpenRouter key minting | Code review: `packages/openclaw-setup-applier/src/openrouter-provisioning.ts` | Minting calls `POST /api/v1/keys` with the profile's `spendLimitUsd`/`limitReset` and the agency management key from env, and returns the minted key without printing it | PASS - `mintOpenRouterKey` POSTs `{name, limit, limit_reset}` with a Bearer auth header to `https://openrouter.ai/api/v1/keys` and returns only the minted key string; test asserts the exact request shape and that a 429 error never leaks its response body. | CONFIRMED - fetched OpenRouter's public API reference (openrouter.ai/docs/api-reference/api-keys/create-api-key, a public doc page, not a live credentialed call) and verified the documented example response has `key` as a top-level field, exactly matching this implementation's `data.key` read; request field names (`name`, `limit`, `limit_reset`) also match the docs exactly. This was previously an unverified assumption; now independently confirmed. | PENDING |
| AC-FN-006 | D12 | `/devops` | Railway variable write path | Code review: `packages/openclaw-setup-applier/src/railway-variables.ts` write path | Setting a variable on the target service calls `railway variable set KEY --service <service> --skip-deploys --stdin --json` via the injectable `RailwayRunner`, piping the value on stdin rather than as an inline `KEY=VALUE` argument, and without printing the value; the `customProviderApiKeyEnv` path omits `--skip-deploys` per the plan's ordering decision | PASS - `writeRailwayVariable` builds exactly this arg list and passes the value only as the `stdin` parameter to `runner.run(args, stdin)`, never in `args`; `skipDeploys` defaults to true and is omitted when explicitly `false`. Required extending `RailwayRunner.run` with an optional `stdin` parameter in `packages/openclaw-railway-installer` (committed separately, `7180e4c`) since the existing interface had no way to pipe input to the spawned CLI process. Tests assert the exact arg list for both branches and that the sentinel value never appears in `args`. | CONFIRMED - code matches; module-level docblock never returns or logs the written value. See AC-SEC-001's QC note for the real-runner-level gap this AC's stubbed test cannot see. | PENDING |
| AC-FN-007 | D13 | `/backend-architect` | Setup API mutating calls | Code review: `packages/openclaw-setup-applier/src/setup-api-client.ts` mutating path | `POST /setup/api/run` and `POST /setup/api/reset` are implemented with the same injectable-`fetch`, status-only-error convention as the read calls | PASS - `run`/`reset` added via a shared `postJson` helper matching `getJson`'s status-only-error convention; both return `unknown` (not independently confirmed live, same as the read calls). Test asserts a 500 response body never leaks into the thrown error. | CONFIRMED - code matches; `postJson`'s comment correctly notes the body may carry a resolved secret and must never reach a thrown error. | PENDING |
| AC-FN-008a | D14 | `/backend-architect` | Idempotency — skip already-configured | Test run: `tests/openclaw-setup-applier-apply-profile.test.ts` "already configured" case | Given a stubbed `GET /setup/api/status` reporting configured, `apply-profile` never calls `POST /setup/api/run` | PASS - test stubs `status: {configured: true}` and asserts the fetch-stub's call-order log contains only `["status"]`, i.e. no `run` call. | CONFIRMED - code and test match; the early return happens before Railway variables are even read. | PENDING |
| AC-FN-008b | D14 | `/backend-architect` | Idempotency — skip already-minted | Test run: `tests/openclaw-setup-applier-apply-profile.test.ts` "secret already present" case | Given a `keyProvisioning` attachment whose declared secret name is already present in stubbed Railway variables, `apply-profile` never calls the OpenRouter mint endpoint for that attachment | PASS - test pre-populates `FakeRailwayRunner` with the fixture's declared secret name already set, asserts the call-order log never contains `"mint"` and that no variable write occurred. | CONFIRMED - code and test match. | PENDING |
| AC-FN-008c | D14 | `/backend-architect` | Ordering — restart-dependent path re-healthchecks | Code review: `packages/openclaw-setup-applier/src/apply-profile.ts` `customProviderApiKeyEnv` path | Writing a variable that feeds `customProviderApiKeyEnv` omits `--skip-deploys` and re-runs the `/setup/healthz` healthcheck before `POST /setup/api/run` is called for that path | PASS - test builds an inline profile with `nonSecretConfig.customProviderApiKeyEnv` set, asserts the write has `skipDeploys: false` and that `"healthz"` appears in the call-order log before `"run"`. A second test confirms the plain (non-custom-provider) path never calls healthz at all. | CONFIRMED-WITH-FIX - while reviewing this path, found that `applyProfile` only ever resolved `modelProviders[0]`, silently dropping any additional attachments with no signal. Correct per issue #7's singular payload fields (one provider per call), but the truncation was implicit. Changed to throw explicitly when `modelProviders.length > 1`, with a new test asserting the throw and that `run` is never reached. | PENDING |
| AC-FN-009 | D15 | `/devops` | CLI entrypoint | Code review: `packages/openclaw-setup-applier/src/cli.ts` | CLI exposes a `--dry-run` flag and an apply mode, following `packages/openclaw-railway-installer/src/cli.ts` argument-parsing conventions | PASS - same `parseArgs` switch-statement shape, `requireValue` helper, and spawn-based `runCommand`/`resolveRailwayExecutable` as the installer CLI (duplicated rather than imported, since the installer doesn't export them — noted in a code comment). Adds `--profile`, `--service`, `--instance-url`, `--dry-run`. No root `package.json` script added yet — judged not-yet-necessary until this CLI has a stable invocation story the team actually wants aliased; deferring is a deliberate choice, not an oversight. | CONFIRMED-WITH-FIX - found a real defect in the duplicated `runCommand`: it streamed every Railway subprocess's raw stdout straight to `process.stdout`, and Railway's own `--help` text documents that `--json`/`--kv` output includes raw variable values — so the first real run of this CLI (dry-run included, since it calls `variable list`) would have reprinted every secret on the target service to the terminal. No test caught it because tests only exercise `FakeRailwayRunner`, never this real spawn path. Fixed: stdout is now captured for parsing only, never echoed; stderr is still echoed (Railway's raw-value warning is documented for `--json`/`--kv` output, not error text — left as-is with a comment rather than guessed at further). See review.md Findings for the full writeup. | PENDING |
| AC-FN-010 | D19 | `/project-manager` | Public positioning updated | Code review: `README.md`, `docs/architecture.md`, `docs/setup-profile-applier.md` | The "a clean place to define setup-profile conventions" line points at the applier concretely; `docs/architecture.md`'s package list includes `packages/openclaw-setup-applier`; none of the three docs names any specific private repo, agency, or client — the profile source is described generically as "a private agency/client profile repo," matching `docs/README.md`'s Public-Repo Rule | PASS - README.md:28 now links `docs/setup-profile-applier.md` and describes the applier concretely; `docs/architecture.md` lists `packages/openclaw-setup-applier`; `docs/setup-profile-applier.md`'s status section now describes both dry-run and apply paths. No private repo/agency/client name introduced in any of the three docs (checked by re-reading all three in full). | CONFIRMED - re-read all three docs; no private name present. Noted in review.md's residual risk that `docs/architecture.md` still omits `packages/openclaw-railway-installer` (pre-existing gap, correctly left out of this plan's scope). | PENDING |

## Security Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-SEC-001 | D16 | `/security` | Secret values never appear in output | Test run: `tests/openclaw-setup-applier-apply-profile.test.ts` sentinel-capture cases — a `--dry-run` and a fully fetch-stubbed / `FakeRailwayRunner`-stubbed apply run, both against a fixture profile (D5) whose secret values are distinctive sentinels (e.g. `sk-test-DO-NOT-LOG-*`), with `console.log`/`console.error` spied | No sentinel value appears in any spied stdout or stderr call across either run | PASS (full) - the full-apply half is now covered: "never logs a real secret value across a full stubbed mint-and-apply run" spies both `console.log` and `console.error` across a full `applyProfile` call (mint → write → run) using `key-provisioning-provider.json`, and asserts neither the minted-key sentinel nor the management-key sentinel appears in any captured line. Combined with the dry-run half (unchanged from Session 1), this row is now fully satisfied. | CONFIRMED, WITH A NAMED GAP - the stubbed test genuinely proves `apply-profile.ts`'s own logic never logs a secret. It cannot see the real spawn-based `RailwayRunner`'s behavior, since every test injects `FakeRailwayRunner`. That real path (`packages/openclaw-setup-applier/src/cli.ts`) DID leak — see AC-FN-009's QC note and review.md Findings — until fixed in this QC pass. This AC's PASS is accurate for the code it can reach; it was never capable of catching the CLI-level leak, and no amount of stubbed-test iteration would have. Flagging this so the gap is explicit in the record, not just fixed silently. | PENDING |
| AC-SEC-002 | D16 | `/security` | Applier writes no local files | Code review: `packages/openclaw-setup-applier/src/apply-profile.ts` and `cli.ts` | Neither module writes to the filesystem; the only outputs are the (redacted) stdout stream and the network calls each secret value serves, per the plan's secret-safety design decision | PASS - neither file imports `node:fs` for writing (`cli.ts` only uses `readFile` to load the input profile); `apply-profile.ts` imports no filesystem module at all. Only outputs are `console.log`/`console.error` and the injected `fetch`/`RailwayRunner` calls. | CONFIRMED - re-checked after the stdout-echo fix; still no filesystem writes anywhere in either file. | PENDING |
| AC-SEC-003 | D17 | `/security` | Tests never call the real OpenRouter API | Code review: `tests/openclaw-setup-applier-*.test.ts` covering D11 | Every test exercising `openrouter-provisioning.ts` stubs `fetch`; no test's assertions depend on a live network call to `openrouter.ai` | PASS - `tests/openclaw-setup-applier-openrouter-provisioning.test.ts`'s 3 tests each pass an explicit `fetchImpl` stub; `tests/openclaw-setup-applier-apply-profile.test.ts`'s mint-triggering tests route through the same shared `buildFetchStub` helper, which throws on any URL it doesn't recognize rather than falling through to the real `fetch`. No test in this feature omits `fetchImpl`/`runner` when exercising a code path capable of a real network or CLI call. | CONFIRMED - independently verified the real OpenRouter response shape (`key` top-level field) against public docs rather than a live call, per AC-FN-005's QC note; this AC's constraint (no test calls the real API) held throughout QC. | PENDING |

## Test Coverage Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-TST-001 | D6 | `/test-engineer` | Session 1 (non-mutating) test run | Test run: `npm.cmd run test -- --run tests/openclaw-setup-applier-profile-schema.test.ts tests/openclaw-setup-applier-setup-api-client.test.ts tests/openclaw-setup-applier-railway-variables.test.ts tests/openclaw-setup-applier-apply-profile.test.ts` | Schema-tolerance, read-client, and dry-run-redaction tests pass, 0 failures | PASS - 4 test files, 15 tests, 0 failures. | CONFIRMED - re-ran full suite post-fix: 11 files, 48 tests, 0 failures (`npm.cmd run test -- --run`). Typecheck and build also re-run clean. | PENDING |
| AC-TST-002 | D17 | `/test-engineer` | Session 2 (mutating) test run | Test run: `npm.cmd run test -- --run tests/openclaw-setup-applier-openrouter-provisioning.test.ts tests/openclaw-setup-applier-cli.test.ts tests/openclaw-setup-applier-setup-api-client.test.ts tests/openclaw-setup-applier-railway-variables.test.ts tests/openclaw-setup-applier-apply-profile.test.ts` | Minting, variable-write, mutating-client, and both idempotency-branch tests pass, 0 failures | PASS - 5 test files, 27 tests, 0 failures. | CONFIRMED - re-ran post-QC-fix (added multi-provider-throw test): same 5 files, now 28 tests, 0 failures. | PENDING |
| AC-TST-003 | D20 | `/test-engineer` | Full project verification | Test runs: `npm run typecheck`, `npm test`, `npm run build` | Typecheck, all tests, and build complete with 0 failures | PASS - typecheck clean; full suite 13 files/65 tests, 0 failures; build clean. | CONFIRMED - re-ran after all QC fixes (stdout-echo fix, multi-provider throw): typecheck clean, full suite 13 files/66 tests 0 failures, build clean, `npm audit --omit=dev` 0 vulnerabilities. | PENDING |

## Regression Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-REG-001 | — | `/test-engineer` | All existing tests pass | Test run: `npm test` | Full suite passes with the new test files included, 0 failures | PASS - 13 test files, 65 tests, 0 failures (up from 11/48 after Session 2's 2 new test files + extended cases). | CONFIRMED - re-ran after Session 2 QC fixes (stdout-echo fix, multi-provider throw + test): 13 files, 66 tests, 0 failures. | PENDING |
| AC-REG-002 | — | `/devops` | Dependency audit stays clean | Test run: `npm audit --omit=dev` | 0 vulnerabilities reported, matching the existing `ci.yml` gate | PASS - `npm audit --omit=dev` reports 0 vulnerabilities; no new dependencies added in Session 2 (only new files in already-dependent packages). | CONFIRMED - re-ran after Session 2 QC fixes; still 0 vulnerabilities, no dependency changes. | PENDING |
