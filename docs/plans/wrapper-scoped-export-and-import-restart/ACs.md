# Wrapper Scoped Export and Import Restart Acceptance Criteria

**Branch:** `feat/wrapper-scoped-export-and-import-restart`
**Plan:** `docs/plans/wrapper-scoped-export-and-import-restart/plan.md`

## Context

Issue #73 adds `GET /setup/export?scope=state` to the pinned Railway wrapper
(state subset only, consistent SQLite snapshot, hard byte cap) and replaces
the `/setup/import` handler's inline `kill → sleep(750)` with the same
exit-confirmed stop helper `restartGateway()` gained in #70. Single session,
D1–D9. No live network access is required for any in-scope AC; the first
real request against a redeployed instance belongs to the consumer repo's
plan and is recorded here as deferred, not passed.

## Column Definitions

| Column | Filled by | When |
| --- | --- | --- |
| Plan ref | Author | At AC authoring; links each row to a plan deliverable ID. |
| Role | Author | At AC authoring; names the role that owns verification. |
| Agent | Verification sub-agent | During AC verification; PASS/FAIL with evidence. |
| QC | Main thread agent | After reading verification evidence; confirms or overrides. |
| Reviewer | Human reviewer | During manual review; final sign-off per AC. |

## Coverage Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-COV-001 | D6 | `/project-manager` | Plan exists | Code review: `docs/plans/wrapper-scoped-export-and-import-restart/plan.md` | Plan names issue, branch, current state (measured sizes, WAL finding, existing handler anchors, no-gate-script exception, branch-collision check), approach, deliverables with roles, design decisions, files to create/edit, gate pre-check, commit schedule, dependencies, out of scope | PASS (main thread) — plan carries issue, branch, current state (measured sizes, WAL, anchors, no-gate exception, collision check), approach, deliverables with roles, design decisions, files, gate pre-check, commit schedule, dependencies, out of scope | CONFIRMED |  |
| AC-COV-002 | D7 | `/project-manager` | ACs table exists | Code review: this file | Every AC row has a valid Plan ref (D1–D9, or `—` for cross-cutting regression rows) and a Role; every deliverable D1–D9 is referenced by at least one row | PASS (main thread) — every AC row has a Plan ref in D1–D9 or `—`, and a Role; every deliverable D1–D9 referenced (manual Gate 1 comm-diff: 0 orphans, 0 dangling) | CONFIRMED |  |
| AC-COV-003 | D8 | `/project-manager` | Review scaffold exists | Code review: `docs/plans/wrapper-scoped-export-and-import-restart/review.md` | Review doc has sections for verdict, deliverables↔code, ACs↔tests, docs drift, recommendations | PASS (main thread) — `review.md` written after verification with verdict, deliverables↔code, ACs↔tests, docs drift, hygiene, recommendations, /retro inputs | CONFIRMED |  |

## Functional Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-FN-001 | D1 | `/devops` | `filterStateEntry` — closed include list | Code review: `scripts/wrapper-state-export.mjs` | Exported `STATE_EXPORT_INCLUDE` and `STATE_EXPORT_EXCLUDE_SEGMENTS`/`STATE_EXPORT_EXCLUDE_BASENAME_PATTERNS` constants exist; `filterStateEntry` returns true only for paths under an include root (and, under `agents/*/agent/`, only `*.sqlite`, `models.json`, `auth-profiles*`); any excluded segment or basename pattern returns false regardless of include; non-regular entries (symlink, socket, device) return false | FAIL (orca verify) — constants, include-root gating, agent allow-list, non-regular rejection all held, but the basename-exclude check was gated on `isFile`: `filterStateEntry('credentials/old.bak-1', dir)` and `('memory/x.migrated', dir)` returned true, so an excluded-named directory under an include root would be walked | CONFIRMED-WITH-FIX — exclude patterns now apply to every segment for files and directories (`segments.some(matchesAny(EXCLUDE_BASENAME_REGEXPS))`); directory decoys added to the fixture and to the direct-predicate test; 21/21 + full suite + docker build green after the fix |  |
| AC-FN-002 | D1 | `/devops` | `snapshotSqlite` — consistent snapshot, never a hot copy | Code review: `scripts/wrapper-state-export.mjs` | Opens the source with `DatabaseSync` (`readOnly`), runs `VACUUM INTO` to the target, closes; `-wal`/`-shm` siblings are never copied; if `import("node:sqlite")` rejects, the function throws with a message naming `node:sqlite` — there is no fallback file-copy branch | PASS — `snapshotSqlite` opens `DatabaseSync(sourcePath, { readOnly: true })`, runs `VACUUM INTO` (single-quote-escaped target), closes in `finally`; `-wal`/`-shm` never copied; `import("node:sqlite")` rejection rethrows naming node:sqlite with no file-copy fallback | CONFIRMED |  |
| AC-FN-003 | D1 | `/devops` | `buildStateExportTree` — byte cap and temp discipline | Code review: `scripts/wrapper-state-export.mjs` | Accumulates bytes as it copies/snapshots and throws once the total exceeds `maxBytes` (default 200 MB, `OPENCLAW_STATE_EXPORT_MAX_BYTES` override) before returning; writes only under `targetRoot/.openclaw/…` so archive paths are relative to `/data`; never follows symlinks | PASS — `buildStateExportTree` accounts bytes on every copy/snapshot and throws once `bytes > cap` before returning (cap = `resolveStateExportMaxBytes()`, env override validated); writes only under `targetRoot/.openclaw/`; readdir dirents + `lstat`, never follows symlinks | CONFIRMED |  |
| AC-FN-004 | D2 | `/devops` | `?scope=state` delegate on the existing route | Code review: `scripts/patch-wrapper-scoped-export.mjs` (the injected text) | The delegate is inserted at the top of the `app.get("/setup/export", requireSetupAuth, …)` handler body; `scope === "state"` builds the tree in an `fs.mkdtempSync` dir and **writes the whole archive to a file in it** (`tar.c({ gzip, portable, noMtime, cwd, file, strict: true, onwarn: log }, [".openclaw"])`) **before any header is set**, then streams that file with `content-type: application/gzip`, `content-length`, and the existing `content-disposition`, removing the temp dir on response `finish`/`close`; any other non-empty `scope` → 400; absent `scope` falls through to the unmodified full-export code path; any failure before headers (cap, `node:sqlite`, tar entry error) → `text/plain` 500 with a one-line reason and no gzip-typed partial body; a read error after headers destroys the response rather than ending it as if complete (amended after Copilot review) | PASS — patch applied to the real pinned `src/server.js` and diffed: delegate inserted immediately after the handler's opening line; `scope === "state"` → mkdtemp, build, same headers, `tar.c({gzip,portable,noMtime,cwd})` on `[".openclaw"]`, temp dir removed on `res` close; other non-empty scope → 400; absent scope falls through to the untouched body; build error → 500 text before headers | CONFIRMED |  |
| AC-FN-005 | D2 | `/devops` | Patch script safety + Dockerfile integration | Code review: `scripts/patch-wrapper-scoped-export.mjs`, `Dockerfile` | Both injections (import line after `import * as tar from "tar";`, delegate after the handler's opening line) use exact-block replace with an exactly-one-occurrence guard and exit non-zero otherwise; the Dockerfile `template-source` stage `COPY`s `scripts/wrapper-state-export.mjs` to `src/wrapper-state-export.mjs`, runs the patch, then asserts with `grep -qF` on the delegate marker and the import line and `node --check src/server.js` | PASS — two exact-block replacements (tar import line; handler opening line) each with an exactly-one-occurrence guard plus an already-applied guard; Dockerfile `template-source` stage copies the module to `src/`, runs the patch, asserts `grep -qF` on the delegate marker and the import line, delegate count `-eq 1`, `node --check` on both files | CONFIRMED — docker build re-run after the AC-FN-001 fix: exit 0 |  |
| AC-FN-006 | D3 | `/devops` | Shared exit-confirmed stop | Code review: `scripts/patch-wrapper-restart-gateway.mjs` (injected text) | A single `async function stopGatewayAndWait()` is defined once (above `restartGateway`), captures the process reference before signalling, resolves on the real `exit` event with a bounded `SIGKILL` escalation, and clears `gatewayProc` only after exit; `restartGateway()` calls it; the import handler's inline `kill/sleep(750)/null` block is replaced by `await stopGatewayAndWait();` — no remaining `sleep(750)` inside either site | PASS — `stopGatewayAndWait()` defined once above `restartGateway` (captures proc, SIGTERM, awaits real `exit` with 5000 ms SIGKILL escalation, then `gatewayProc = null`); `restartGateway()` calls it; the import handler's inline block replaced by `if (gatewayProc) { await stopGatewayAndWait(); }`; no `sleep(750)` remains at either site | CONFIRMED |  |
| AC-FN-007 | D3 | `/devops` | Per-site occurrence guards | Code review: `scripts/patch-wrapper-restart-gateway.mjs`, `Dockerfile` | Each of the two target blocks (restartGateway body, import-handler stop block) is matched independently and the script exits non-zero if either occurs ≠ 1 times; the Dockerfile asserts `grep -qF 'stopGatewayAndWait'` and that the count of `await sleep(750);` in `src/server.js` dropped by exactly the number of replaced sites relative to the pinned template | PASS — three labelled replacements each counted independently; any count ≠ 1 exits non-zero naming the label; Dockerfile asserts `grep -qF 'stopGatewayAndWait'`, definition count 1, call count 2, and `await sleep(750);` count `-eq 2` (4 → 2 measured against the pinned file) | CONFIRMED |  |
| AC-FN-008 | D2, D3 | `/devops` | Unscoped behaviour preserved | Code review + `docker build --target template-source` | With no `scope` query the export handler's original body is byte-identical to the pinned template's (the delegate is a prefix, not a rewrite); `/setup/import`'s extraction and restart semantics after the stop are unchanged | PASS — `git diff --no-index` of pinned vs doubly-patched `src/server.js`: exactly four hunks (tar import, restartGateway, export handler prefix, import handler stop block); the export handler's original body and `/setup/import`'s extraction/restart semantics are byte-identical | CONFIRMED |  |

## Test Coverage Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-TST-001 | D4 | `/test-engineer` | Include/exclude invariants on a real fixture | Test run: `npm test -- --run tests/openclaw-railway-wrapper-patches.test.ts` | Against a temp `STATE_DIR` containing every include root plus decoys (`bin/big.bin`, `lib/x`, `agents/main/sessions/s.jsonl`, `agents/main/agent/plugins/p.js`, `openclaw.json.bak-1`, `state/openclaw.sqlite-wal`, a symlink), the produced tree's relative paths are all accepted by `filterStateEntry`, none contain an excluded segment/basename, and the symlink is absent | PASS — set-equality between the produced tree and `files`; every produced path re-accepted by `filterStateEntry`; no excluded segment/basename; decoys + symlink absent; `describe.skipIf` block ran (confirmed by test name) | CONFIRMED — directory decoy `credentials/old.bak-1/secret.txt` added after AC-FN-001; still 21/21 |  |
| AC-TST-002 | D4 | `/test-engineer` | SQLite snapshot is consistent | Test run: same file | A WAL-mode fixture DB created with `node:sqlite` (rows inserted, WAL left un-checkpointed) is snapshotted; opening the snapshot yields the inserted rows and `PRAGMA integrity_check` returns `ok`; no `-wal`/`-shm` file exists next to the snapshot | PASS — WAL-mode fixture with an open writer and `wal_autocheckpoint=0`; snapshot reopened, rows present, `integrity_check` = ok; no `-wal`/`-shm` beside the snapshot | CONFIRMED |  |
| AC-TST-003 | D4 | `/test-engineer` | Byte cap throws before completion | Test run: same file | With `maxBytes` smaller than the fixture total, `buildStateExportTree` rejects with an error naming the cap and the target root contains no partial archive artifact beyond the tree itself | PASS — real `buildStateExportTree` with a cap smaller than the fixture rejects naming the cap and env var; target contains only the bare tree | CONFIRMED |  |
| AC-TST-004 | D4 | `/test-engineer` | Patch scripts apply once and refuse twice | Test run: same file | On a synthetic `server.js` fixture containing the exact anchor lines, each patch script exits 0 and `node --check` passes on the result; running the same script again on the patched fixture exits non-zero with the occurrence count in its message | PASS — both scripts spawned for real on a synthetic fixture: first run exit 0 + `node --check` ok, second run non-zero with the occurrence message; independently re-measured against the real pinned template | CONFIRMED |  |
| AC-TST-005 | D4 | `/test-engineer` | Import-handler replacement is site-specific | Test run: same file | After D3's script runs on the fixture, the two unrelated `await sleep(750);` sites — in the pinned template the **2nd and 3rd** occurrences (`gateway.stop` and `POST /setup/api/reset`; corrected from "third and fourth" after verification) — remain byte-identical and only the restartGateway (1st) and import-handler (4th) sites changed | PASS on the invariant — remainder-diff proves only the restartGateway and import-handler sites changed; the two unrelated sites survive verbatim. AC text corrected: the survivors are the 2nd/3rd occurrences (`gateway.stop`, `POST /setup/api/reset`), not "third and fourth" | CONFIRMED-AMENDED — Pass cell reworded; fixture constant renamed `RESET_STOP_BLOCK_UNRELATED` |  |

## Documentation Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-DOC-001 | D5 | `/devops` | Dockerfile rationale + README note | Code review: `Dockerfile`, `README.md` | Each new/extended patch step has a comment block stating the defect, the measured sizes, why the fix is build-time, and links to #73 and the upstream drafts; README's wrapper `/setup` section documents `?scope=state`, the archive layout (`.openclaw/…` relative to `/data`), the byte cap env var, and that the archive is importable by `/setup/import` | PASS — Dockerfile: both steps carry contiguous comment blocks (defect, measured 541 MB vs ~7 MB, WAL/VACUUM INTO, build-time rationale, #73, upstream drafts). README: `### Scoped state export` subsection under `## OpenClaw on Railway` documents `?scope=state`, the `.openclaw/…` archive layout relative to `/data`, `OPENCLAW_STATE_EXPORT_MAX_BYTES`, unknown scope → 400, importable by `/setup/import` | CONFIRMED |  |
| AC-DOC-002 | D5 | `/devops` | Documentation hygiene | Code review: diff of docs | No hosted Railway project/service/org identifiers, URLs, or tenant names in any new doc content (measured sizes are fine; instance identity is not) | FAIL (orca verify) — one hit: `agency-prod` (hosted instance name) in plan.md Approach item 5; everything else clean on a full read of all new doc content; no secret values anywhere | CONFIRMED-WITH-FIX — reworded to "its agency-instance redeploy deliverable"; branch-wide sweep for private repo/org/tenant/Railway identifiers now returns nothing |  |
| AC-DOC-003 | D9 | `/project-manager` | Upstream issue drafts ready for the operator | Code review: `docs/plans/wrapper-scoped-export-and-import-restart/upstream-issues.md` | Two drafts (scoped export request; import-path race referencing upstream #233) with reproduction facts and the minimal fix shape; file states they are **not yet filed** and names the operator as the filer; after filing, URLs are appended | PASS — `upstream-issues.md` opens with **Status: NOT FILED**, names the operator as filer, carries two drafts (scoped export request; import-path race referencing upstream #233) with reproduction facts and fix shape, and a `Filed:` line per issue for the URL | CONFIRMED — URLs to be appended by the operator |  |
| AC-OPS-001 | D2 | `/devops` | Live `?scope=state` request (deferred to consumer) | Manual, post-merge; not run in this session | The consumer repo's plan runs the first real request during its agency-instance redeploy deliverable and records `401` unauthenticated / `200` + `application/gzip` authenticated / `content-length` ≪ cap; this row is DEFERRED-to-consumer, not PASS, until that evidence is linked here | DEFERRED-to-consumer — no live request in this session by design; the consumer plan's agency-instance redeploy AC records `401`/`200 application/gzip`/size | CONFIRMED-DEFERRED |  |

## Regression Acceptance Criteria

| AC | Plan ref | Role | What | How | Pass | Agent | QC | Reviewer |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| AC-REG-001 | — | `/test-engineer` | All existing tests pass | Test run: `npm test` | Full suite passes with the new test file included, 0 failures (pre-existing Postgres-gated skips allowed) | PASS — `npm test`: 48 files passed / 5 skipped, 425 tests passed / 30 skipped (pre-existing Postgres-gated), 0 failures | CONFIRMED |  |
| AC-REG-002 | — | `/devops` | Typecheck and build stay clean | Test run: `npm run typecheck`, `npm run build` | 0 type errors, build succeeds | PASS — `npm run typecheck` clean; `npm run build` runs as `pretest` and succeeded | CONFIRMED |  |
| AC-REG-003 | — | `/devops` | Patched wrapper stage builds | `docker build --target template-source .` | Stage builds successfully; both patch scripts print their success line; all `grep -qF`/`node --check` assertions pass | PASS — `docker build --target template-source .` exit 0 after the fixes: both patch scripts print their success line; every `grep -qF`/`test`/`node --check` assertion passes; image removed | CONFIRMED |  |
| AC-REG-004 | — | `/project-manager` | Diff scope check | `git diff origin/main --stat` | Changed files are exactly the plan's Files to Create/Edit lists (excluding `CHANGELOG.md`/`package.json`, which change at `/commit`) — no extras | PASS — `git status` lists exactly the plan's Files to Create/Edit (module, two patch scripts, test, Dockerfile, README, plan/ACs/review/upstream-issues); no extras | CONFIRMED |  |

---

## Agent Notes

`/agentic-orca` run `wf_92d228bd-f7d` (2026-08-27): CG1 (D1/D2/D3 + Dockerfile comments) → then CG2 (D4 tests) and CG3 (README + upstream drafts) in parallel; 6 agents, 0 errors. Verify: 15 PASS / 2 FAIL (AC-FN-001 directory-exclude gap; AC-DOC-002 hosted instance name in plan.md). CG1 dry-ran both patch scripts against the real pinned `src/server.js` (`sleep(750)` 4 → 2), smoke-tested D1 on a WAL fixture, and ran the docker `template-source` build. Assumed, not verified here: `DatabaseSync({ readOnly: true })` on the runtime image's exact Node version — the consumer plan's live redeploy AC confirms it.

## QC Notes

Fix count after verification: 2 code/docs + 2 wording. (1) `filterStateEntry` now applies the basename-exclude patterns to every path segment for files and directories; directory decoys added to the fixture and the direct-predicate test (AC-FN-001). (2) `agency-prod` removed from plan.md (AC-DOC-002). (3) AC-TST-005's Pass cell corrected to the pinned template's real site ordering; fixture constant renamed `RESET_STOP_BLOCK_UNRELATED`. Re-verified: `npm run typecheck`, the test file 21/21, `npm test` 425 passed / 30 pre-existing skips, `docker build --target template-source` exit 0. No re-run of the orca pipeline was needed; the main thread re-executed the exact commands the failing rows named.

## Reviewer Feedback

*Human fills this section during review.*
